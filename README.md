# Capstone
build location model

## Assets

- `assets/models/spaghetti_bowl.glb` — a realistic, procedurally generated
  3D object: a colorful nest of spaghetti on a plate. Open
  `assets/models/index.html` (served over HTTP) for an interactive
  drag-to-orbit / AR viewer. See `assets/models/README.md` for details and
  how to regenerate it via `scripts/generate_spaghetti_bowl.py`.
